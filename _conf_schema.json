{
  "debug_mode": {
    "description": "启用调试日志",
    "type": "bool",
    "default": false,
    "hint": "开启后将输出详细的 Debug 级别日志，方便排查问题。正常使用时请关闭"
  },
  "llm_provider": {
    "description": "LLM 提供者",
    "type": "string",
    "default": "astrbot",
    "options": [
      "astrbot",
      "openai_compatible"
    ],
    "hint": "astrbot(使用AstrBot内置LLM) / openai_compatible(使用自定义OpenAI兼容API)"
  },
  "llm_api_base": {
    "description": "OpenAI兼容API地址",
    "type": "string",
    "default": "",
    "hint": "当llm_provider为openai_compatible时填写，例如 https://api.openai.com/v1 或其他兼容API地址"
  },
  "llm_api_key": {
    "description": "OpenAI兼容API密钥",
    "type": "string",
    "default": "",
    "hint": "当llm_provider为openai_compatible时填写API Key"
  },
  "llm_model": {
    "description": "OpenAI兼容API模型名称",
    "type": "string",
    "default": "gpt-4o-mini",
    "hint": "当llm_provider为openai_compatible时填写模型名，如 gpt-4o-mini, deepseek-chat 等"
  },
  "enable_miniapp_detect": {
    "description": "启用B站链接自动识别",
    "type": "bool",
    "default": false,
    "hint": "开启后，机器人会自动识别群内分享的B站视频链接（包括小程序、卡片、短链等），推送封面和视频信息"
  },
  "detect_show_cover": {
    "description": "识别推送：显示封面图",
    "type": "bool",
    "default": true,
    "hint": "识别到B站链接时，是否在推送中包含视频封面图片"
  },
  "detect_show_uploader": {
    "description": "识别推送：显示UP主名",
    "type": "bool",
    "default": true,
    "hint": "识别到B站链接时，是否显示UP主名称"
  },
  "detect_show_desc": {
    "description": "识别推送：显示简介",
    "type": "bool",
    "default": true,
    "hint": "识别到B站链接时，是否显示视频简介（超100字自动截断）"
  },
  "detect_show_pubtime": {
    "description": "识别推送：显示发布时间",
    "type": "bool",
    "default": true,
    "hint": "识别到B站链接时，是否显示视频发布时间"
  },
  "detect_show_link": {
    "description": "识别推送：显示BV号链接",
    "type": "bool",
    "default": true,
    "hint": "识别到B站链接时，是否显示完整视频链接"
  },
  "detect_show_stats": {
    "description": "识别推送：显示播放数据",
    "type": "bool",
    "default": true,
    "hint": "识别到B站链接时，是否显示播放量、弹幕、点赞等数据"
  },
  "detect_auto_summary": {
    "description": "识别推送：自动生成总结",
    "type": "bool",
    "default": false,
    "hint": "识别到B站链接时，是否自动生成视频总结（注意：会消耗LLM额度）"
  },
  "access_mode": {
    "description": "群聊访问控制模式",
    "type": "string",
    "default": "blacklist",
    "options": [
      "blacklist",
      "whitelist"
    ],
    "hint": "blacklist(黑名单模式: 列表中的群禁止使用) / whitelist(白名单模式: 仅列表中的群可使用)"
  },
  "group_list": {
    "description": "群号列表（逗号分隔）",
    "type": "string",
    "default": "",
    "hint": "黑名单/白名单的群号列表，用英文逗号分隔，如 123456,789012"
  },
  "output_image": {
    "description": "总结以图片形式发送",
    "type": "bool",
    "default": true,
    "hint": "开启后总结将渲染为精美图片发送（需要安装 wkhtmltopdf），关闭则发送纯文本"
  },
  "check_interval_minutes": {
    "description": "定时检查新视频间隔 (分钟)",
    "type": "int",
    "default": 600,
    "min": 5,
    "max": 1440,
    "hint": "多长时间检查一次订阅UP主是否有新视频，单位分钟"
  },
  "note_style": {
    "description": "总结风格",
    "type": "string",
    "default": "professional",
    "options": [
      "concise",
      "detailed",
      "professional"
    ],
    "hint": "concise(简洁 — 仅核心要点) / detailed(详细 — 完整记录，适合学习) / professional(专业 — 结构化分析与深度总结)"
  },
  "enable_link": {
    "description": "启用原片跳转链接",
    "type": "bool",
    "default": true,
    "hint": "是否在总结中插入原片跳转时间戳链接"
  },
  "enable_summary": {
    "description": "启用AI总结",
    "type": "bool",
    "default": true,
    "hint": "是否在总结末尾加入AI生成总结"
  },
  "download_quality": {
    "description": "音频下载质量",
    "type": "string",
    "default": "fast",
    "options": [
      "fast",
      "medium",
      "slow"
    ],
    "hint": "fast(快速32kbps) / medium(中等64kbps) / slow(高质128kbps)"
  },
  "processing_timeout": {
    "description": "处理超时时间 (秒)",
    "type": "int",
    "default": 300,
    "min": 60,
    "max": 600,
    "hint": "单个视频总结生成的最大超时时间"
  },
  "max_note_length": {
    "description": "总结最大字符数",
    "type": "int",
    "default": 3000,
    "min": 500,
    "max": 10000,
    "hint": "总结最大字符数，超过将自动截断（防止消息过长发送失败）"
  },
  "enable_auto_split": {
    "description": "长视频自动分图",
    "type": "bool",
    "default": true,
    "hint": "视频内容过长时，自动将总结拆分为多张图片输出，避免内容被截断"
  },
  "max_cards_per_image": {
    "description": "每张图最多卡片数",
    "type": "int",
    "default": 6,
    "min": 2,
    "max": 12,
    "hint": "每张总结图片最多包含多少个章节卡片，建议6-8个以保证图片大小适中"
  },
  "enable_forward_message": {
    "description": "总结以合并转发发送",
    "type": "bool",
    "default": false,
    "hint": "开启后，视频信息和AI总结将打包为合并转发（聊天记录）形式发送，包含封面、视频信息和完整总结"
  },
  "enable_auto_push": {
    "description": "启用自动推送",
    "type": "bool",
    "default": false,
    "hint": "是否启用定时检查和自动推送新视频信息"
  },
  "auto_push_summary": {
    "description": "自动推送：推送视频总结",
    "type": "bool",
    "default": true,
    "hint": "启用后，定时检查触发的自动推送会进行总结；关闭后仅发送视频基本信息（标题、封面、简介、链接等）"
  },
  "max_subscriptions": {
    "description": "最大订阅数",
    "type": "int",
    "default": 20,
    "min": 1,
    "max": 100,
    "hint": "每个会话最多订阅多少个UP主"
  },
  "push_groups": {
    "description": "推送QQ群列表",
    "type": "string",
    "default": "",
    "hint": "新视频总结推送到哪些QQ群，用英文逗号分隔，如 123456,789012。也可通过 /添加推送群 命令添加"
  },
  "push_users": {
    "description": "推送QQ号列表",
    "type": "string",
    "default": "",
    "hint": "新视频总结推送到哪些QQ号（私聊），用英文逗号分隔。也可通过 /添加推送号 命令添加"
  },
  "default_count": {
    "description": "搜索默认数量",
    "type": "int",
    "default": 20,
    "min": 1,
    "max": 50,
    "hint": "AI调用搜索列表功能时，如果未指定数量，则使用此默认值"
  },
  "default_download_count": {
    "description": "建议下载数量",
    "type": "int",
    "default": 3,
    "min": 1,
    "max": 20,
    "hint": "建议AI每次下载转写的视频数量，非硬性限制"
  },
  "search_max_concurrent": {
    "description": "搜索下载并发数",
    "type": "int",
    "default": 1,
    "min": 1,
    "max": 5,
    "hint": "同时进行下载和转写的视频数量，数量越大服务器压力越大"
  },
  "search_show_progress": {
    "description": "搜索下载显示进度",
    "type": "bool",
    "default": true,
    "hint": "AI搜索下载视频时，是否显示实时进度消息（如：进度 1/3）"
  },
  "prefer_subtitle": {
    "description": "优先使用字幕",
    "type": "bool",
    "default": true,
    "hint": "视频有字幕时优先获取字幕内容进行总结，没有字幕再下载音频转写（可节省时间和资源）"
  }
}